version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "8081:80"
    volumes:
      - ./restaurant-images:/usr/share/nginx/html/restaurant-images:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
